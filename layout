HOME

Header img - lleva a #

Header-buttons - 

	si auth	-> username | logout
	Si no auth	-> login | register

menu-container
	si auth -> menu
	si no auth -> vacío

App-container
	-> imagen pong

footer
	-> footer

LOGIN

Header img

Header-buttons
	si auth	->  usernamer | logout
	Si no auth	-> register

menu-container
	vacío

App-container
	Si no auth -> formulario login
	Si auth -> vacío o mensaje de usuario ya logueado

footer -> footer

Redireccion -> home

REGISTER

Header img

Header-buttons
	si auth	-> usernamer | logout
	Si no auth	-> login

menu-container
	vacío

App-container
	Si no auth -> formulario register
	Si auth -> vacío o mensaje de usuario ya registrado

footer -> footer

Redireccion -> home

LOGOUT

Header img

Header-buttons
	->  login | register

menu-container
	vacío

App-container
	Si no auth -> No creo que pueda llegar a este punto pero si lo hace a traveś de url mensaje de usuario no logueado
	Si auth -> vacío o mensaje de usuario deslogueado con exito o error

footer -> footer

Redireccion -> home


RESTO DE STEPS

Header img

Header-buttons
	si auth	-> usernamer | logout
	Si no auth	-> login | register

menu-container
	si auth -> menu
	si no auth -> vacío

App-container
	Si no auth -> mensaje de usuario no logueado
	Si auth -> REnder de cada step

footer -> footer

Redireccion -> Dependerá del usuario a menos que eliminemos el mensaje de usuario no logueado y redirgamos directamente a home.